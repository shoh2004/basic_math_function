/*
    Create function named func with a argument
    Convert the number from radians to degrees.
    Multiply one number by the sine of another number.
    Args:
        y  (int): the number to be converted
        x (int): multiply x by the sine of y
    Returns:
        result (double): multiply x by the sine of y. Round the result to two decimal places.
*/
import 'dart:math';

void main() {
  print(func(8, 27));
}

func(x, y) {
  return x * sin(0.47);
}
